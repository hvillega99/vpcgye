<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include("templates/header.ejs") %>
    <title>Iniciar sesión</title>
</head>
<body>

    <div class="container p-2">
        <div class="row justify-content-center">
            <div class="col-md-3">

                <% if(showMessage) { %>

                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <%= message%>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>

                <% } %>

                <div class="text-center">
                    <img src="/img/vpc-logo.jpg" class="img-fluid pb-2 h-50 w-50" alt="vpc"></img>
                </div>
                <div class="card">
                    <div class="card-header">
                        <h5 class="card-title text-center">Iniciar sesión</h5>
                    </div>
                    <form class="card-body" method="post" action="/auth/signin">
                        <div class="mb-3">
                            <label htmlFor="input-cedula" class="form-label">Cédula</label>
                            <input name="cedula" type="text" class="form-control" id="input-cedula" required></input>
                        </div>
                        <div class="mb-3">
                            <label htmlFor="input-password" class="form-label">Contraseña</label>
                            <input name="password" type="password" class="form-control" id="input-password" required></input>
                        </div>
                        <div class="text-center">
                            <button type="submit" class="btn btn-primary">Entrar</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    
</body>
</html>